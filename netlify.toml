[build]
  command = "vite build"
  publish = "dist/client"

[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888

[functions]
  directory = "dist/server" # Đây là nơi Netlify sẽ tìm các hàm serverless

[[redirects]]
from = "/*"
to = "/.netlify/functions/server"
status = 200
